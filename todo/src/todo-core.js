// Pure to-do core logic (no I/O, no CLI parsing)

import {
  manageTodos,
  getEndOfToday,
  isDuplicateTask,
  addTask,
  markTaskDone,
  removeTask,
  listTasks,
  PRIORITY_HIGH,
  PRIORITY_NORMAL,
  normalizeText,
  normalizeDueDate,
  sortTodosForList
} from '../../src/todo-core.js';

export {
  manageTodos,
  getEndOfToday,
  isDuplicateTask,
  addTask,
  markTaskDone,
  removeTask,
  listTasks,
  PRIORITY_HIGH,
  PRIORITY_NORMAL,
  normalizeText,
  normalizeDueDate,
  sortTodosForList
};


